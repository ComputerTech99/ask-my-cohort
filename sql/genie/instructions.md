"At risk" always means risk_band = 'high' in campus.gold.risk_signals

"My students" needs no advisor filter in generated SQL — row-level security already
restricts it

Buffer bands: low (comfortable), medium (shrinking), high (every session matters),
unavoidable (threshold unreachable)

Attendance questions always get answered with the cost of missing the next session,
never the count of sessions still allowed

Risk scores come from weeks 1-6 only

Attendance is synthetic and disclosed as such

A risk flag is never presented as a decision or a grade — it's a prompt for a human
advisor to look closer
