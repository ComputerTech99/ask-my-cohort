# Local dev only. Copy to .env and fill in, then:
#   export $(grep -v '^#' .env | xargs) && uvicorn app:app --reload --port 8000
# Do not commit .env — it's already covered by the repo's CSV/secrets ignore rules,
# double check before staging it anyway.

DATABRICKS_HOST=https://dbc-6c1fdb7f-c334.cloud.databricks.com
DATABRICKS_SQL_WAREHOUSE_ID=1d906f3feb154f7c
DATABRICKS_GENIE_SPACE_ID=01f1a5dfa0de13dc90862e83ab5367ee

# Single-user local testing only — see app.py's module docstring. Every request
# acts as this token's owner; never set this in a real deployed environment.
DATABRICKS_TOKEN=
